#!/bin/bash
# Post-push hook to remind about workflow documentation updates

# Check if spec ID was set during pre-push
if [ -f .git/PUSH_SPEC_ID ]; then
    RELATED_SPEC=$(cat .git/PUSH_SPEC_ID)
    rm .git/PUSH_SPEC_ID

    # Color codes
    GREEN='\033[0;32m'
    YELLOW='\033[1;33m'
    BLUE='\033[0;34m'
    NC='\033[0m' # No Color

    echo ""
    echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
    echo -e "${GREEN}✓ Push successful!${NC}"
    echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
    echo ""
    echo -e "${YELLOW}📝 Documentation Reminder:${NC}"
    echo ""
    echo "Related spec: $RELATED_SPEC"
    echo ""
    echo "Please ensure the following are updated:"
    echo ""
    echo "  1. PM Workflow:"
    echo "     mkdocs-docs/engineering/pm-workflows/${RELATED_SPEC}.md"
    echo ""
    echo "  2. Dev Workflow:"
    echo "     mkdocs-docs/engineering/dev-workflows/[timestamp]-*.md"
    echo ""
    echo "  3. Spec file (if needed):"
    echo "     project/specs/${RELATED_SPEC}.md"
    echo ""
    echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
    echo ""
fi

exit 0
